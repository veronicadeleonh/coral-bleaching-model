services:
  - type: web
    name: coral-bleaching-api
    runtime: python
    buildCommand: pip install -r api/requirements.txt
    startCommand: cd api && gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
